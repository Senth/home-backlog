import { View } from "react-native";
import { Button } from "react-native-paper";

export const Screen = () => (
	<View>
		<Button mode="contained">one</Button>
		<Button mode="contained">two</Button>
	</View>
);
